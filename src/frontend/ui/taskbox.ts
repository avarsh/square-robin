import { Task } from "../../types/task";

export function buildTaskBox(task: Task): string {
  
  let template: string = `\
    <div class='taskbox'>\
      <h2>Hello</h2>\
    </div>\
  `;
  
  return template;
}